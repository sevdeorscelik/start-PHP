<?php

// current();       - Gibt das aktuelle Element in einem Array zurück
// end();           - Setzt den internen Zeiger eines Arrays auf sein letzes Element
// next();          - Erhöht den internen Zeiger eines Arrays
// prev();          - Ruft den internen Array-Zeiger ab.
// reset();         - Positioniert den internen Zeiger eines Arrays auf das erste Element
// extract();       - Variablen in einem Array in die Symboltabelle aufnehmen
// asort();         - Sortiert die Werte eines Arrays vom kleinesten zum größten, ohne ihre Beziehung zu sein Keys zu unterbrechen.
// arsort();        - Sortiert die werte eines arrays vom größten zum kleinsten, ohne ihre beziehung zu sehen keys zu unterbrachen
// ksort();         - Sortiert ein Array nach seinen Keys vom kleinsten zum größten.
// krsort();        - Sortiert ein array nach keys vom größten zum kleinsten. 
